<!DOCTYPE html>
<html>
  <head>
    <title>WarehouseCellStorage</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">

      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
     
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" href="/items">List of parts</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="/items/new">New part</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="/addresses/new">New address</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="/stocks/new">Add stock</a>
              </li>
            </ul>    
          </div>
          <% if user_signed_in? %>
            <div class="navbar-text me-2"><%= current_user.username %></div><%= button_to 'Sign Out', destroy_user_session_path, :method => :delete, class: "btn btn-outline-secondary" %>
          <% else %>
            <%= button_to 'Sign in', new_user_session_path, class: "btn btn-outline-secondary" %> 
          <% end %>
        </div>
      </nav>

      <div>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>

        <%= yield %>
      </div>

    </div>
  </body>
</html>
